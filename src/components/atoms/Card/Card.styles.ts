import styled from 'styled-components';
import { Card as AntCard } from 'antd';

/**
 * Base styled card component
 * Provides basic card styling with transitions
 */
export const StyledCard = styled(AntCard)`
    transition: all 0.3s;
`; 