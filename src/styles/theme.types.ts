export interface Theme {
    colors: {
        background: string;
        primary: string;
        text: string;
        textPrimary: string;
        textSecondary: string;
        surface: string;
    };
    borderRadius: string;
    spacing: {
        sm: string;
        md: string;
        lg: string;
    };
} 